<?xml version="1.0" encoding="UTF-8"?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             HasSystemPadding="{Binding IsSystemPadding}"
             Padding="{Binding Padding}"
             x:Class="FastTalkerSkiaSharp.Pages.ModifyPage">

        <StackLayout BackgroundColor="Black"
                     Padding="5,5,5,5"
                     Opacity="0.95">
            <Grid BackgroundColor="White"
                  HorizontalOptions="FillAndExpand"
                  VerticalOptions="FillAndExpand">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Text="Modify"
                       HorizontalTextAlignment="Center"
                       FontSize="Large"
                       FontAttributes="Bold"
                       Grid.Row="0"
                       Grid.Column="0"
                       Grid.ColumnSpan="3"/>

                <!-- Reset Icon -->
                <StackLayout Margin="5"
                             Grid.Row="1"
                             Grid.Column="0"
                             VerticalOptions="FillAndExpand"
                             HorizontalOptions="FillAndExpand">
                    <Button x:Name="btnResetSize"
                            Text="Reset Size"
                            VerticalOptions="FillAndExpand"
                            HorizontalOptions="FillAndExpand"
                            BorderColor="{StaticResource GlobalBorderColor}"
                            BackgroundColor="{StaticResource GlobalButtonColor}"
                            Command="{Binding ButtonResetSize}"
                            BorderWidth="1"
                            BorderRadius="2"/>
                </StackLayout>

                <!-- Increase Icon Size + 10% -->
                <StackLayout Margin="5"
                             Grid.Row="1"
                             Grid.Column="1"
                             VerticalOptions="FillAndExpand"
                             HorizontalOptions="FillAndExpand">
                    <Button x:Name="btnGrow"
                            Text="Increase Size 10%"
                            VerticalOptions="FillAndExpand"
                            HorizontalOptions="FillAndExpand"
                            BorderColor="{StaticResource GlobalBorderColor}"
                            BackgroundColor="{StaticResource GlobalButtonColor}"
                            Command="{Binding ButtonIncreaseSize}"
                            BorderWidth="1"
                            BorderRadius="2"/>
                </StackLayout>

                <!-- Increase Icon Size + 50% -->
                <StackLayout Margin="5"
                             Grid.Row="1"
                             Grid.Column="2"
                             VerticalOptions="FillAndExpand"
                             HorizontalOptions="FillAndExpand">
                    <Button x:Name="btnGrow2"
                            Text="Increase Size 50%"
                            VerticalOptions="FillAndExpand"
                            HorizontalOptions="FillAndExpand"
                            BorderColor="{StaticResource GlobalBorderColor}"
                            BackgroundColor="{StaticResource GlobalButtonColor}"
                            Command="{Binding ButtonIncreaseSize2}"
                            BorderWidth="1"
                            BorderRadius="2"/>
                </StackLayout>

                <!-- Edit Icon Text -->
                <StackLayout Margin="5"
                             Grid.Row="2"
                             Grid.Column="0"
                             VerticalOptions="FillAndExpand"
                             HorizontalOptions="FillAndExpand">
                    <Button x:Name="btnEditText"
                            Text="Edit Icon Text"
                            VerticalOptions="FillAndExpand"
                            HorizontalOptions="FillAndExpand"
                            BorderColor="{StaticResource GlobalBorderColor}"
                            BackgroundColor="{StaticResource GlobalButtonColor}"
                            Command="{Binding ButtonEditText}"
                            BorderWidth="1"
                            BorderRadius="2"/>
                </StackLayout>

                <!-- Decrease Icon Size - 10% -->
                <StackLayout Margin="5"
                             Grid.Row="2"
                             Grid.Column="1"
                             VerticalOptions="FillAndExpand"
                             HorizontalOptions="FillAndExpand">
                    <Button x:Name="btnShrink"
                            Text="Decrease Size 10%"
                            VerticalOptions="FillAndExpand"
                            HorizontalOptions="FillAndExpand"
                            BorderColor="{StaticResource GlobalBorderColor}"
                            BackgroundColor="{StaticResource GlobalButtonColor}"
                            Command="{Binding ButtonDecreaseSize}"
                            BorderWidth="1"
                            BorderRadius="2"/>
                </StackLayout>

                <!-- Decrease Icon Size - 50% -->
                <StackLayout Margin="5"
                             Grid.Row="2"
                             Grid.Column="2"
                             VerticalOptions="FillAndExpand"
                             HorizontalOptions="FillAndExpand">
                    <Button x:Name="btnShrink2"
                            Text="Decrease Size 50%"
                            VerticalOptions="FillAndExpand"
                            HorizontalOptions="FillAndExpand"
                            BorderColor="{StaticResource GlobalBorderColor}"
                            BackgroundColor="{StaticResource GlobalButtonColor}"
                            Command="{Binding ButtonDecreaseSize2}"
                            BorderWidth="1"
                            BorderRadius="2"/>
                </StackLayout>

                <!-- Edit Pinning Status -->
                <StackLayout Margin="5"
                             Grid.Row="3"
                             Grid.Column="0"
                             VerticalOptions="FillAndExpand"
                             HorizontalOptions="FillAndExpand">
                    <Button x:Name="btnPin"
                            Text="Pinning Icon"
                            VerticalOptions="FillAndExpand"
                            HorizontalOptions="FillAndExpand"
                            BorderColor="{StaticResource GlobalBorderColor}"
                            BackgroundColor="{StaticResource GlobalButtonColor}"
                            Command="{Binding ButtonPinning}"
                            BorderWidth="1"
                            BorderRadius="2"/>
                </StackLayout>

                <!-- Edit Pinning Status -->
                <StackLayout Margin="5"
                             Grid.Row="3"
                             Grid.Column="1"
                             VerticalOptions="FillAndExpand"
                             HorizontalOptions="FillAndExpand">
                    <Button Text="Delete Icon"
                            VerticalOptions="FillAndExpand"
                            HorizontalOptions="FillAndExpand"
                            BorderColor="{StaticResource GlobalBorderColor}"
                            BackgroundColor="{StaticResource GlobalButtonColor}"
                            Command="{Binding ButtonDelete}"
                            BorderWidth="1"
                            BorderRadius="2"/>
                </StackLayout>
            </Grid>
        </StackLayout>
</pages:PopupPage>
